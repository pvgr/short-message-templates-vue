export const activePanel = (state) => {
  return state.activePanel;
}

export const currentlyEditing = (state) => {
  return state.currentlyEditing;
}

export const geocodingApiKey = (state) => {
  return state.geocodingApiKey;
}

export const lang = (state) => {
  return state.lang;
}

export const locale = (state) => {
  return state.locale;
}

export const location = (state) => {
  return state.location;
}

export const snippets = (state) => {
  return state.snippets;
}

export const templates = (state) => {
  return state.templates;
}

export const toolbarsAreVisible = (state) => {
  return state.toolbarsAreVisible;
}
